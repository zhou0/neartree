interface:
  display_name: "Neartree"
  short_description: "Repo-specific patterns and workflows for neartree"
  default_prompt: "Use the neartree repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true